{{#each states}}
  <div class="col-3 my-1 stateColumn" id="state-{{id}}" onload="loadState({{id}})">
     <div class="font-weight-bold text-center my-3 mt-4">{{name}}</div>
     <div class="stateTasks">

     </div>
   </div>
   {{/each}}
